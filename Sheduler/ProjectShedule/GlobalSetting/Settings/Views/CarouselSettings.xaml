<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ProjectShedule.GlobalSetting.Settings.ViewModels"
             xmlns:views="clr-namespace:ProjectShedule.GlobalSetting.Base.Views"
             x:Class="ProjectShedule.GlobalSetting.Settings.Views.CarouselSettings"
             x:DataType="local:CarouselSettingsViewModel">
  <ContentView.Content>
      <Grid 
          RowSpacing="0" 
          BackgroundColor="{DynamicResource ViewBackgroundColor}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label 
              Grid.Row="0"
              Text="{Binding Header}"
              Style="{DynamicResource MediumLabelStyle}"/>
          <StackLayout
              Grid.Row="1"
              CompressedLayout.IsHeadless="True">
              <views:RangeStepperView BindingContext="{Binding YearsRangeSettingViewModel}"/>
          </StackLayout>
        </Grid>
  </ContentView.Content>
</ContentView>