<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels ="clr-namespace:ProjectShedule.GlobalSetting.ViewModels"
             xmlns:views="clr-namespace:ProjectShedule.GlobalSetting.Views"
             x:Class="ProjectShedule.GlobalSetting.Views.SheduleDeleteQuestionSettingView"
             x:DataType="viewModels:SheduleDeleteNotesSettingModel">
  <ContentView.Content>
      <Grid 
          RowSpacing="0" BackgroundColor="{DynamicResource ViewBackgroundColor}">
          <Label 
              Grid.Row="0"
              Text="{Binding Title}"
              Style="{DynamicResource MediumLabelStyle}"/>
          <StackLayout 
              Grid.Row="1"
              CompressedLayout.IsHeadless="True">
              <views:SwitchSettingView BindingContext="{Binding DeleteQuestionSetting}"/>
          </StackLayout>
      </Grid>
  </ContentView.Content>
</ContentView>